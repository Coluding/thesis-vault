---
last_updated: 2026-08-06
status: living
---

# Experiment results ledger

Single reverse-chronological index of **every experiment result that actually
ran** (this folder's per-run notes). Purpose: reuse for the thesis at a glance —
what we have, with sources, and which section each result feeds.

**Conventions**
- One row per result note in this folder. Newest first (by the note's `date:`).
- **Every metric traces to a run** (wandb id + ckpt + commit) per CLAUDE.md hard
  rules 7–8. Headlines here are pointers; the full sourced numbers live in the
  linked note. `_nv_` = the note's own source is marked `_needs verification_`.
- **Thesis §** = where the result is *actually cited* in `70_Thesis/draft/`.
  `—` means **not yet used in the draft** (a reuse gap to close, not an error).
- Add a row whenever you promote a completed run into a note here (never for a
  planned run — those are tickets under `20_Tickets/experiments/`).

## Results

| Date | Run (wandb) | Base × Dataset | D | Headline (sourced in note) | Thesis § | Note |
|---|---|---|---|---|---|---|
| 2026-08-06 | slurm `25240257` (V5 diff) vs `25241732` (V5.1 flow) | **EasyAnimate V5 / V5.1 7B** × ACWM Robot Arm | **D2** | ✅ **FINAL @ matched step 14,200.** (1) **The Wan ceiling was NOT intrinsic to output adapters** — the same 34.9M adapter cuts denoising loss **−83.8% / −68.7%** vs the frozen base and contributes **0.52 / 0.49** of the prediction, against **−3.3%** and **0.047** on the best Wan arm; `adapter_base_cosine` 0.87 vs Wan's **0.9989** (i.e. on Wan the adapted prediction was numerically almost the base). (2) **The objective's effect is far larger on ACTION-SPECIFICITY than on capacity** — `effect_rel` **+55%** (0.0600 vs 0.0387) and `effect_vs_adapter` **+47%** (0.116 vs 0.079), against only **+5%** on raw contribution. Diffusion led **all 15** hourly evals, never once behind, and is **~4× more stable** (last three ±0.001 vs flow 0.030–0.044). Cross-backbone: both diffusion bases (DC, EA-V5) sit above both flow bases (EA-V5.1, Wan) — n=2 per objective across independent families. ⚠ single matched-step readings, not means over draws. ⚠ EA-flow (0.039) is ~4× Wan-flow, so it is "flow is weaker", NOT "flow fails". ⚠ only **8–12%** of the adapter's output is action-driven — the domain-corrector question stays open at ~10× scale. ⚠ V5/V5.1 differ in TEXT stack (BERT+T5 vs Qwen2VL) and CFG steers through text | — | [[20260806-objective-governs-action-specificity-not-adapter-capacity]] |
| 2026-08-07 | slurm `25302987`/`25302988` (wandb `uusbz707`/`75fikn1t`) + gen `25313706`, diag `25315512` | **DynamiCrafter 1.4B** × ACWM Robot Arm | **D3** | **CAPACITY-MATCHED PDD: the adapter's failure is NOT capacity.** A (additive adapter) and B (LoRA'd base) matched to **0.02%** on trainable params (146,373,664 vs 146,405,408), same objective/grid/bs/lr/data. A's eval_loss improved **4× (0.524 → 0.128)** and reached the same level ~3× faster in steps, yet its 1-call rollout is **still unusable** (noise texture, no scene) while B at **5× worse loss (~0.64)** still produces a recognisable arm. ⭐ **The stated degenerate-fixed-point mechanism is REFUTED**: predicted low-variance rollouts, measured latent std vs GT = **1.009-1.027 on 16/16 clips** (B is the odd one at 2.21-2.24). ⭐ **FOUR latent-space signals rank the arms backwards** (PSNR A 17.85>B 16.33; latent MSE A 0.44<B 2.0; implied latent corr A ~0.78 vs B ~0; plus seq50<seq8) while pixels say B wins — **no latent scalar we have tried can rank these models**. Remaining untested asymmetry: B's heads read features from the 1.4B PRETRAINED backbone, A's from a from-scratch network. ⚠️ `best.pt` is selected on eval_loss, which on arm A rewards the failure | — | [[20260806-pdd-parallel-decoding-works-on-the-lora-base-not-the-adapter]] |
| 2026-08-06 | slurm `25262886`/`25262887`/`25264058` + gen `25284685` (⚠️ no wandb) | **DynamiCrafter 1.4B** × ACWM Robot Arm | **D3** | **PDD parallel decoding works on the LoRA'd base (Idea B), NOT on the additive adapter (Idea A).** One network call vs 8, same noise/conditioning/grid: **B emits a recognisable arm scene; A emits a featureless blur** (qualitative, panels in `outputs/pdd-generations/`). ⭐ **PSNR-vs-GT ranks this BACKWARDS** — A par 18.37 > seq8 17.72 > seq50 16.88, and A beats B 18.37 vs 16.02; the in-run proof is seq50 < seq8 on every arm, since generation-from-noise is not reconstruction and blur sits near the conditional mean. **Do not rank generative quality with PSNR on this task.** Training loss is equally misleading: matched (bs8/lr5e-5) A and B are indistinguishable (mean eval_loss 0.570 vs 0.567) and A's PDD loss fell across ALL 8 heads (k000 10.7×) without yielding a usable rollout — the degenerate fixed point, observed. ⭐ Matched-batch cost: **A 20.3 GiB / 0.08 steps/s vs B 39.8 GiB / 0.05** (B ~2× VRAM, ~1.6× slower; B OOMs at A's bs=24 because the peak is the retained student graph PLUS a concurrent teacher forward). ⚠️ visual read from 1 clip/arm — FID/FVD needed. ⚠️ an earlier run pair was discarded: mean-velocity target ~1000× too small (grid in timestep units vs velocity in normalised time) | — | [[20260806-pdd-parallel-decoding-works-on-the-lora-base-not-the-adapter]] |
| 2026-08-06 | `mo3k2639` (slurm 25259766) — motion confirmation for `jlnl7s1k` | **Wan2.2-TI2V-5B-Turbo (distilled, 4-step)** × ACWM Robot Arm | D2 | **The paired shuffled-action control fires; the frozen-base control does not.** The 2026-08-05 hand-measured claim (adapted r=+0.75 vs base +0.09, gap **0.66**) does NOT survive instrumentation: logged adapted−base is +0.13/+0.10/+0.045/**−0.034** across 4 draws (n=16, steps 400 & 800) — sign-inconsistent, centred on zero. The frozen base tracks per-clip GT motion about as well as the adapter. **What DOES hold is the causal control**: `motion_corr_action_gain` (adapted − shuffled-action, same weights, same conditioning frame, same seed) is positive in **4/4 draws** — +0.069, +0.168, +0.122, +0.213, mean **+0.143**, sign test p≈0.06 one-tailed. So a modest, consistently-signed action effect on per-clip motion magnitude. ⚠️ **Do not quote a number yet**: the logged 95% CIs are on `corr(adapted, GT)`, NOT on the gain — the causal quantity has no interval. ⚠️ adapted correlation itself spans zero in 3/4 draws. ⚠️ `action_loss_gap` still ≈0, so this is motion *magnitude*, not *correctness* | — | [[20260806-motion-tracking-is-action-driven-but-the-base-control-was-wrong]] |
| 2026-08-05 | `jlnl7s1k` (slurm 25240927) vs `zcjvjj5a` | **Wan2.2-TI2V-5B-Turbo (distilled, 4-step)** × ACWM Robot Arm | D2 | **Binning action tokens to the latent grid raises action EFFECT 4–6.5× but not action ACCURACY.** Prior runs cross-attended 97 action tokens against 25 latent frames (`PASSTHROUGH`) — token *j* matched no frame, the documented 2026-08-02 failure. With `action_seq_len: latent`: `effect_rel` 0.00313→**0.01268** and `vs_adapter` 0.0277→**0.179** at matched step 1600, growing monotonically to 0.307. **But `action_loss_gap` stays ≈0 (|x|≤0.00055, all ten evals)** — the action changes the prediction without the *correct* action reducing error. ⚠️ two variables moved (binning + 34M→100M): needs a 34M+binned control. ⚠️ overfits from step 1200 (eval_loss .127→.192). ⚠️ A hand-measured n=6 signal that adapted motion tracks GT per clip is **stronger than first assessed** (Spearman ρ=**0.943**, p≈0.017; rank order matches GT in **2/2 independent draws**, chance 1/36 — base matches 1/2 = chance). Open question is now **causal, not statistical**: action, or conditioning frame? The paired shuffled-action control (`eval/motion_corr_action_gain`) settles it | — | [[20260805-turbo-action-tokens-binned-to-latent-grid]] |
| 2026-08-03 | `25183721` (LoRA) vs `vy9tcuco` | Wan × ACWM Robot Arm (**adapter-family cost**) | **D1** | **LoRA costs 3.1× the wall-clock per optimizer step of an output adapter** on the same frozen 5B, at matched effective batch (66 vs 205 steps/h) — and LoRA is the *smaller* adapter (26.0M vs 34.9M). Structural: its trainables sit INSIDE the base, so the pass must be differentiable through all 30 blocks, and the activation memory then forces gradient checkpointing. **The cost number backing D1's cheap-and-composable claim**, and the counterweight to the frozen-vs-finetuned objection | — | [[20260803-lora-costs-3x-the-walltime-of-an-output-adapter]] |
| 2026-08-03 | `6ruz55f6` vs `vy9tcuco` | Wan × ACWM Robot Arm (**concat** vs cross-attention injection) | D2 | 🛑 **NEGATIVE: channel-concat does NOT beat cross-attention.** Below it at every matched step (0.00975 vs 0.01056 @1200; share 0.168 vs 0.181) with identical `eval_loss`. Does **not** reproduce GigaWorld-1's 2.2× — their concat carries a spatially-aligned control *video*, ours broadcasts a 7-DoF vector uniformly, so it adds no spatial information cross-attention lacked. Built + kept: per-dim standardisation (raw actions RMS 0.054 vs ~unit latents = **18× too quiet**) | — | [[20260803-concat-injection-does-not-help]] |
| 2026-08-02 | `52o3uxz8` `vy9tcuco` `6oyu1inq` `0fqjrqjl` | Wan **and** DC × ACWM/RT-1 | D2/D1 | **THE HEADLINE: the same adapter is a DOMAIN adapter on Wan and an ACTION conditioner on DC — never both.** Wan×ACWM beats the frozen base 6/6 (**FVD −64%**, 1118→406) with `effect_rel` 0.006 and all three structure probes **at chance**; DC arm E has real control (steering +0.117, alignment 1.000, spatial 0.470, all above chance). Scope: the Wan quality win is ACWM-only (RT-1 is a perceptual regression). Plus the **D1 composition-interface ablation** — the oracle buys quality (FVD +63.7% vs +45.1%) and costs 25% of action-following | — | [[20260802-adapter-is-a-domain-adapter-not-an-action-conditioner]] |
| 2026-08-02 | slurm 25154218 (offline benchmark) | Wan **and** DC × ACWM Robot Arm (rollout timing) | D3 | **wall-clock is linear in N**: Wan 17.6 s + **3.09 s/step**, DC 2.5 s + **1.10 s/step**. ⚠ **DC is 2.8× faster per step — that is model size (1.4B vs 5B), NOT the objective**, so "flow is faster" must not be claimed. For **latent-space** planning the fixed term is paid once, so few-step speedup is **linear, 50×, no floor** (an earlier "capped at 10×" reading applied only to decoded video). CEM 30×5 on DC: **~1.15 h/planning-step at N=50 → ~1.4 min at N=1** | — | [[20260802-rollout-wallclock-vs-steps]] |
| 2026-08-02 | probes `25156497` `25155284` `25155716` `25156498` | **{Wan flow, DC diffusion} × {shortcut, control}** × ACWM Arm (action-free) | D3 | ⚠️ **CONFOUNDED cross-base (arms differ in consistency target AND depth) — within-arm results stand.** Shortcut is learnable in the Wan/flow/`v_average` cell and not in the DC/diffusion/`endpoint_inversion` cell. Depth-matched @800: Wan treated `consistency_cos` **0.302** [0.251,0.356] vs control **0.034** [0.026,0.042] — **9×, non-overlapping**; DC @400 (also matched): 0.084 vs control **0.083** — **none, CIs coincident**. Second independent signal, the gain profile: Wan treated **flat O(1)** across the ladder (0.33 at d=1/2), Wan control **collapses** (0.026), DC **explodes to 4e4**. Clip-nulls near-identical across the Wan pair (0.0170/0.0166) so not a genericity artifact; base-null exactly 0. **First clean D3 positive**; supersedes the gate-bug-confounded 68×. ⚠️ mechanism only — few-step *quality* not measured (qualitatively 'not yet good, right direction'; scoped to future work, budget exhausted) | — | [[20260802-shortcut-works-on-flow-not-diffusion]] |
| 2026-08-02 | `avid_wan_rt1_47M_faithful` / `..._pooled` (in flight); probes `25148083`/`25148084` | **AVID clean-room** (3rd branch of the official repo) × Wan2.2 TI2V-5B × RT-1 | D2 | **per-frame action addressability is CAUSAL, and Wan is NOT the harder substrate.** Single-variable A/B at matched step 5000, matched adapter contribution (0.114 vs 0.111) and matched mask (0.953): per-frame conditioning gives **1.7× more action-driven** contribution (share 15.3% vs 9.1%; effect_rel 0.01747±0.00153 vs 0.01019±0.00159, Welch t=3.30, n=16 batches), null 0. **And at MATCHED step 5000 the faithful arm BEATS the AVID/DynamiCrafter reference** (0.01747 vs 0.01254, re-probed `25148170`) — the famous 0.0495 is a **step-15000** number, so Wan is *not* the harder substrate. What differs is purity: AVID 24.4% action-driven vs ours 15.3%. **Gain-vs-information confound RESOLVED** by a temporal-control probe: diagonal concentration **0.390 vs 0.199 (chance 0.200)** — the pooled arm's per-frame response rows are *bit-identical*. **@step 10000 the gap WIDENS to 2.46x** (0.02764 vs 0.01124, Welch t=7.3; share 23.0% vs 9.1%; diag conc 0.409 vs 0.198): **@12000: 2.49x** (0.03178 vs 0.01279, t=10.5, contributions identical to 0.2%). Faithful **+82%** over 5k→12k and share 15.3%→**25.3%**; pooled +25% and share 9.1%→10.2% — an **information ceiling**, not under-training. Faithful's 25.3% now **exceeds** AVID's 24.4% | — | [[20260802-avid-wan-cleanroom-perframe-causal]] |
| 2026-08-01 | SIMPLE arm (killed @~3500, ckpt step_00002800 retained) | **simple 7.5M transformer** × ACWM (no oracle, enforced px→latent binning) | D2 | **negative result: a structurally clean adapter does not escape the ACWM floor** — settles at ~0.0025 (band 0.002–0.007), *below* the DiT-clone arms' 0.008–0.011 → the DiT capacity/inductive bias is worth 3–4× on this data; architecture class alone neither causes nor cures the blindness | — | (logged in [[20260731-wan-action-signal-is-a-global-bag]] §fixes + this row) |
| 2026-08-01 | `skyreels-rt1-TOKENNORM-NOBASE` + `gi44pv5k` (both killed) | **SkyReels** × RT-1 | D2 | 🛑 **RETRACTED 2026-08-02 — the whole cell is void.** `dataset_size=76`, not 5000 (`temporal_length: 97` vs RT-1's ~22–115-frame episodes; `dataset.py:71` silently dropped 98.5%), **and** eval was in-sample. The 0.0450, the '91% of reference', the **35×** and the SkyReels quality numbers are all void. Re-run needs `temporal_length: 17` + a held-out split | — | [[20260801-wan-rt1-indistribution-plateau]] |
| 2026-08-01 | `5w72bo01` (killed @25.9 h) | Wan × **RT-1** (token-norm + no-oracle) | D2 | settles ~0.021 (peak 0.0332) vs ACWM's ~0.011 → read as dataset-dependent action economics. **⚠ PROVISIONAL — same confound** (`effect_rel` cannot separate action information from early-fit instability; both predict peak-then-settle *and* the data axis). **Quality: FID 132.5 vs base 113.2, LPIPS 0.382 vs 0.365 — worse; PSNR 17.5 vs 16.5, MSE 0.0176 vs 0.0222 — better** | — | [[20260801-wan-rt1-indistribution-plateau]] |
| 2026-07-31 | probe of NOBASE ckpt (job 25107536) | Wan × ACWM Robot Arm (steering/temporal/spatial analysis) | D2 | **sensitivity-without-control explained: the action signal is a GLOBAL BAG** — steering cos **0.00** (map arbitrary), temporal alignment **0.25 = chance** (px→latent correspondence never formed; tokens are per pixel frame, DiT per latent frame), spatial concentration 11% ≈ chance. Fix: explicit temporal binning implemented + SIMPLE arm relaunched (25107616) | — | [[20260731-wan-action-signal-is-a-global-bag]] |
| 2026-07-31 | `6oyu1inq` `tr0uovs5` `86kb01su` (final @step 3500) | DC × ACWM Robot Arm (condition_center long-run) | D2 | **arm E finishes 0.11479** (the 0.106→0.092 dip was noise). ⚠ **the level claim is retired**: untreated arm 0 reaches **0.04564** and arm F **0.05049** — *both above* the AVID reference unaided — and the E-over-control gap fell **3.7×→2.5×** in the final 500 steps without converging. **Only the ~6× faster escape survives.** ⚠ `effect_rel` is monotone in action-path gain and `condition_center` is a gain knob — structure triad (job 25144197) decides whether this is a mechanism fix. **No quality metrics logged on any DC run** | — | [[20260731-dc-condition-center-accelerates-escape]] |
| 2026-07-31 | GATEFIX · `52o3uxz8` · NOBASE (in flight) | Wan × ACWM Robot Arm (token-norm / nobase) | D2 | **token-norm sustained 6–10× control but erodes** (0.0113→0.0067) with the oracle present; **NOBASE stops the erosion** (flat 0.0106, pre-registered ✓); plateau ~0.011. Videos @3000: adapted finally distinct from base, **20% closer to GT**, motion-timing corr 0.67 vs base 0.17. Rollout-action-swap probe running (25104155) | — | [[20260731-wan-tokennorm-nobase-training-results]] |
| 2026-07-31 | probes of 4 Wan arms + attribution job 25097452 | Wan × ACWM Robot Arm (copy decomposition) | D2 | **why Wan copies the base, decomposed:** (1) shared-target convergence — cosine 0.87 even with NO base input (high cosine ≠ copying); (2) the oracle — adapter's prediction is **~100× more sensitive to base_pred than to actions** (drel 1.13 vs 0.009); (3) economics — **actions worth 0.45% of the loss**; (4) pull scales with composition weight (0.99 @ 50% vs 0.93 @ 10%). Explains the DC controls' late escape + predicts NOBASE trajectory | — | [[20260731-why-wan-copies-the-base-decomposed]] |
| 2026-07-31 | probe of `ncztxyyo` (trace job 25085110) | Wan × ACWM Robot Arm (23-depth action trace) | D2 | **located WHERE Wan loses the action: survives cross-attention (xattn out 44–56% action-driven, all 10 blocks) then DROWNS at the residual add** — xattn RMS ~0.01 vs stream 1.8–3.0, drel 0.44→0.0085 in one addition; value pathway unnormalised while qk_norm rescues the logits. Mirror of DC's pedestal. σ-sweep: effect_rel flat (mismatch hypothesis dead). Fix arm `action_token_norm` launched (25085598) vs GATEFIX control | — | [[20260731-wan-action-trace-value-pathway-drowns]] |
| 2026-07-30 | `n3dbgq4q` `l2jcz9nx` `hbuu4lwx` `1e0fe9ei` `2us8hugq` `t62nhyfu` | ours DC × ACWM Robot Arm (6-arm parity) | D2 | **all 4 parity treatments null** vs a measured noise floor (0.000245) — best arm 0.0043 vs 0.02 target. **Mechanism found:** our action embedding is 14× larger than time_emb but **99.7% constant** (realised/RMS **0.0050** vs AVID **0.238**, 48×), and the pedestal is **LEARNED** — at init 0.182×/2.4% varying, by step 600 magnitude ×106 and varying fraction ÷7 | — | [[20260730-dc-parity-arms-null-action-embedding-pedestal]] |
| 2026-07-30 | `rqp4s3gp` (AVID Robot Arm) | AVID recipe × ACWM Robot Arm (official) | D2 | **AVID FOLLOWS actions on the dataset OUR adapters went blind on** (effect_rel 0.0295 @step5000, ~42% action-driven, null 0) vs ours 0.0056/0.0034/0.0013 → **blindness is OUR IMPLEMENTATION, not the data**. Contrast: our DC adapter contributes **4.6× more** (rel_contrib 0.319 vs 0.070) yet is **1.5% action-driven vs AVID's 42%** — contributing the wrong thing, not cloning. ⚠ not training-matched (5000 vs ~800–1200) | — | [[20260730-avid-robotarm-follows-actions-recipe-not-data]] |
| 2026-07-29 | `pzmc2orq` (Wan) `t4bp8nki` (DC) | shortcut · flow vs diffusion · ACWM Arm | D3 | early curvature signature: **DC consistency loss ~68× Wan's** (diffusion much harder to make self-consistent); few-step quality + step-size probe still open | — | [[20260729-shortcut-wan-vs-dc-curvature-signature]] |
| 2026-07-29 | `93qrvr5v` (AVID RT-1) | AVID recipe × RT-1 (official) | D2 | AVID follows actions on in-distribution RT-1 (effect_rel 0.0495, ~66% action-driven, null 0). ⚠ **its "blindness is a DATA/OOD problem" conclusion is SUPERSEDED by `20260730`** — the measurement stands, the inference does not | — | [[20260729-avid-rt1-follows-actions-control]] |
| 2026-07-28 | `ncztxyyo` `c3pcewxk` `8zjjn7wl`; `423pjv8y`(AVID ref — ⚠ Push Cube, `max_clips: 64`, memorization-**confounded**, not a usable reference) | Wan/DC/SkyReels × ACWM Robot Arm | D2 | **all three adapters action-blind on Robot Arm** (effect_rel ~0, cos~1, null-violation 0) despite 8.7× residual — 3 distinct starvation signatures | — | [[20260728-acwm-robotarm-matrix-action-blind]] |
| 2026-07-25 | local diag (no wandb) | Wan5B × ACWM Arm/Cube | D2 | frozen-base residual **0.314 (Arm) vs 0.036 (Cube)**, ≈8.7× — base-parity is a flat-visuals problem | §5.1.y _(proposed, not yet written)_ | [[20260725-acwm-base-residual-diagnostic]] |
| 2026-07-24 | `rxzwh4ak` `o79ki0ul` `o9113j4h`(failed) `hvxlbfjx`; `uxrst2k5`(ref) | Wan5B × MetaWorld | D2 | base-parity persists after both optimization traps fixed (cos 0.86, denoise Δ≈0, FID within 0.1 of base) | §5.1.x | [[20260724-metaworld-cap-shift-triangle-base-parity]] |
| 2026-07-21 | `y1jrgxqp` `uxrst2k5` | Wan5B × MetaWorld | D2 | replace-noise root cause validated; total action-blindness of the xattn adapter (σ-sweep + action probe) | §5.1.x | [[20260721-replace-fix-validation-sigma-sweep-action-probe]] |
| 2026-07-16 | `bcipghvw` `uea10230` `5cxstyh4` | Wan5B × MetaWorld | D2 | three xattn runs: adapter converges to clone the base, not to use actions (wandb-API pulled) | — | [[20260716-wan-xattn-adapter-clones-base-not-actions]] |
| 2026-07-15 | `pg3x72uc` | AVID-native × MetaWorld | expl | AVID native reference run — gate healthy (positive control) | — | [[20260715-avid-metaworld-native-gate-healthy]] |
| 2026-07-12 | `xb76ptw2` | Wan5B × MetaWorld | D2 | xattn action injection worse than base on every eval metric (killed @2661) | — | [[20260712-wan-xattn-action-no-improvement]] |
| 2026-07-09 | _nv_ | Wan5B(pretrained) × MetaWorld | D4 | first genuinely-pretrained frozen base — coherent samples, weak action tracking; base_loss flat ~0.10–0.20 | — | [[20260907-flow-shortcut-weak-action-signal]] |
| 2026-06-29 | _nv_ | flow vs diffusion × MetaWorld | D3 | first post-pivot sample batch — loss ok, sample videos poor (blur/fog/colour drift) | — | [[20260629-flow-vs-diffusion-shortcut-samples]] |
| 2026-06-27 | _nv_ | Wan5B AVID no-shortcut × MetaWorld | D2 | action-conditioned AVID adapter without shortcut consistency (ablation) | — | [[wan22-avid-noshortcut-ablation]] |
| 2026-06-17 | _nv_ (proj `avid-shortcut-metaworld-0.45`) | AVID-shortcut × MetaWorld (large) | D3 | volatile shortcut loss diagnosed as a step-size-mixing artifact (resolved by per-step-size logging) | — | [[avid-shortcut-anchor045-volatile-loss]] |

## Not results (planning / protocol — kept here but not indexed as runs)

- [[protocol-param-matched-adapter-comparison]] — param-matched cross-family
  comparison **protocol** (D1/D2), not a run.

## Reuse gaps (rows with Thesis § = —)

These are completed results **not yet cited in the draft**. Candidates to pull
in when writing the relevant section:
- D2 base-parity build-up (`20260716`, `20260712`) → context for §5.1.x.
- D3/D4 shortcut + flow (`20260629`, `20260617`, `20260709`) → §5.2 / §5.3 (stubs).
- The **base-residual diagnostic** (`20260725`) → proposed §5.1.y (results) +
  §4.4 base-selection (SkyReels probes are qualitative, logged in
  [[../../60_Updates/entries/2026-07-25-base-parity-is-flat-visuals-robotarm-residual]]).
