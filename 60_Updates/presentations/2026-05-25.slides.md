# Agenda

- Progress since last week
- Key finding: frame-stride conditioning
- Open decision + the one blocker
- Next steps

---

### Progress
# What moved this week

- Worked through the **frame-stride (`fs`) conditioning** path end-to-end
- Opened and reasoned through a design decision on per-sample stride sampling
- Stood up the vault's **progress-log + presentation** pipeline (this deck)

---

### Key finding
# `fs` conditions the base, not the adapter

- `fs` reaches the **frozen base UNet**, but `Δ_φ` (the trainable adapter) sees it only *indirectly*, via base activations
- The MetaWorld dataset emits a **constant** stride (default `1`) — no variation to learn from
- A precedence bug feeds the *slice stride* into the base's *fps* channel

> Net: for D2, frame stride is currently neither learnable nor varied — worth fixing before trusting any `fs`-related behaviour.

---

### Decision
# Per-sample stride sampling — design settled, one gate

::: columns
**Resolved**

- `fs` = augmentation / nuisance → base-only, not `Δ_φ`
- Actions aggregate by **sum** under stride `k` (all dims are deltas)
- Stride range = swept hyperparameter
|||
**Open gate**

- Feed base `5/k` (effective fps) or raw `k`?
- Hinges on DynamiCrafter's pretrained fps-vs-fs convention
- *Needs verification before implementation*
:::

---

# Next steps

- Verify the DynamiCrafter fps/fs convention — unblocks the sampler
- Decide whether to fix the precedence bug independently now
- Continue the param/FLOPs-matched adapter comparison (blocked on the FLOPs-per-step estimator)

<!-- notes: Lead with the finding; the decision is mostly settled, only the DC-convention check is outstanding. -->

---

# Discussion
