
## 16:20 — Wan's action signal is measurable but not *visible*: a mean-regression problem

Lukas, 2026-08-03. Refinement of the Wan verdict worth keeping distinct:

**The Wan action signal is measurable in the prediction — it just does not
manifest in the generated videos.** The adapter's prediction responds to the
action (effect_rel nonzero, cross-attn output 44–56% action-driven at every
depth), but the rendered rollout does not show it. Read as **mean
regression**: the model hedges toward the conditional mean, which is the
same signature already logged on RT-1 (better PSNR/MSE, worse FID/LPIPS).

So the honest Wan statement is *"present, measurable, and structureless —
and additionally suppressed at render time by mean regression"*, not
"absent".

**Fixes exist and are future work — the family Lukas calls _exploration_.**
To be discussed properly later; candidate experiment. Plausible relatives:
action-CFG (sharpens the conditional away from the mean), rollout/multi-step
losses, action-conditional consistency — all already named in
[[../50_Decisions/open/wan-action-following-needs-objective-change]] as
objective-level levers.

➜ ACTION: discuss, then decide whether this becomes an experiment ticket or
a future-work paragraph. Do not write the mean-regression claim into the
draft until the render-vs-prediction gap is measured explicitly on Wan
(currently inferred from the RT-1 metric split, not directly probed).
